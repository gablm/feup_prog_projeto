#include "XPM2.hpp"
#include <fstream>
#include <sstream>
#include <map>

namespace prog {
    Image* loadFromXPM2(const std::string& file) {
        return nullptr;
    }

    void saveToXPM2(const std::string& file, const Image* image) {

    }
}
